/** EPIC-045 `T1624` — injection tokens for the artifact store (data-model.md §1–§3). */
export const ARTIFACT_STORE = Symbol('ARTIFACT_STORE');
/** The specification entity, reached through a port the specifications module implements (`R-045-4`). */
export const SPECIFICATION_SYNC_PORT = Symbol('SPECIFICATION_SYNC_PORT');
