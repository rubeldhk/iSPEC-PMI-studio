- One specification per Epic.
- An Epic whose task estimate exceeds {taskCeiling} MUST be split before /speckit-tasks; a split is
  proposed by the agent and confirmed by a human.
- An Epic whose task estimate exceeds {taskCeiling} is split by the agent along functional seams
  before /speckit-tasks; no confirmation is required by this project's policy.
- Policy version {version}.
