# DEF-029-001 — `pnpm lint` was not clean when this Epic's V2 gate first ran it

**Originating task**: T878 (quickstart V2 requires `pnpm lint` clean before the literal-value rule's cleanliness means anything)
**Found**: 2026-08-21, first full lint run of the EPIC-029 worktree
**Status**: RESOLVED (2026-08-21, this Epic's implement run)

## Reproduction

`pnpm lint` at `049806a` (before any EPIC-029 change) exits non-zero with three
errors, none of them from EPIC-029 files:

1. `backend/src/modules/reviews/gate-execution.service.ts:119` — `'err' is defined but never used`
2. `backend/src/modules/storage/publish.service.ts:16` — `'StorageProvider' is defined but never used`
3. `frontend/src/components/JobProgress.tsx:51` — disable comment names `react-hooks/exhaustive-deps`, a rule from a plugin this repository does not install; ESLint 9 errors on unknown rules in directives

## Expected vs actual

Expected: lint green on main — quickstart V2's "adding a literal makes lint
fail" is only evidence if the baseline is clean. Actual: the baseline itself
failed, in EPIC-023/024/025 code delivered 2026-08-21; that delivery verified
typecheck and the test suites but did not run `pnpm lint`.

## Resolution

Mechanical, behavior-preserving fixes applied in this Epic's worktree because
V2 is this Epic's gate and cannot pass over them: `err` renamed `_err` (the
catch deliberately discards it), the unused `StorageProvider` type import
dropped, the no-op disable comment removed. No logic changed; the full unit
suite still passes. Flagged in the closing report as three files outside
EPIC-029's scope.
