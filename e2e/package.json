{
  "name": "e2e",
  "private": true,
  "description": "End-to-end journey measurements (SC-001). Requires a running stack — see tests/sc-001-journey.spec.ts for the exact run steps.",
  "scripts": {
    "test": "playwright test"
  },
  "devDependencies": {
    "@playwright/test": "^1.49.0"
  }
}
